import React from "react";

const Wrapper = ({ children }) => {
	return <div style={{ width: "1170px", maxWidth: "100%", marginLeft: "auto", marginRight: "auto" }}>{children}</div>;
};

export default Wrapper;
